server:
  port: 9092

spring:
  profiles:
    active: dev
  application:
    name: food-catalogue
  datasource:
    url: jdbc:mysql://localhost:3306/foodcataloguedb
    username: root
    password: getdog
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true